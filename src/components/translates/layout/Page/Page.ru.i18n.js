import { PAGE as idPrefix } from '../../../constants/translates';

const messages = {
  [`${idPrefix}.welcome`]: 'Нас ждут великие дела',
  [`${idPrefix}.home`]: 'Главная',
  [`${idPrefix}.about`]: 'О нас',
  [`${idPrefix}.lazy`]: 'Lazy',
  [`${idPrefix}.memo`]: 'Memo',
  [`${idPrefix}.context`]: 'Context',
};

export default messages;

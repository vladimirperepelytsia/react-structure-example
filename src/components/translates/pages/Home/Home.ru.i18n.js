import { HOME as idPrefix } from '../../../constants/translates';

const messages = {
  [`${idPrefix}.page`]: 'Главная страница',
  [`${idPrefix}.newJoke`]: 'Новая шутка'
};

export default messages;

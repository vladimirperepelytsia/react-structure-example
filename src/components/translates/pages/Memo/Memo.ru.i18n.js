import { MEMO as idPrefix } from '../../../constants/translates';

const messages = {
  [`${idPrefix}.page`]: 'React memo страница',
  [`${idPrefix}.title`]: 'Название',
  [`${idPrefix}.production`]: 'Производитель',
  [`${idPrefix}.like`]: 'Лайк',
  [`${idPrefix}.likes`]: 'Лайки'
};

export default messages;

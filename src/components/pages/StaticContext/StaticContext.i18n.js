import { defineMessages } from 'react-intl';
import { STATIC_CONTEXT as idPrefix } from '../../constants/translates';

const messages = defineMessages({
  page: {
    id: `${idPrefix}.page`,
    defaultMessage: 'Static Context page'
  }
});

export default messages;

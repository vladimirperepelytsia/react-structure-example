import { defineMessages } from 'react-intl';
import { ABOUT as idPrefix } from '../../constants/translates';

const messages = defineMessages({
  page: {
    id: `${idPrefix}.page`,
    defaultMessage: 'about page'
  }
});

export default messages;
